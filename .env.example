# GitHub push + PR settings (template)
# Create a private .env and fill real values:
#   install -m 600 .env.example .env

# Required: token with repo scope (classic) or fine-grained repo contents:write + pull_requests:write.
GITHUB_TOKEN=ghp_replace_me

# Required: your fork remote URL (the repo you own).
FORK_REMOTE_URL=https://github.com/your-username/opencode-multi-auth-codex.git

# Optional: if you prefer a custom remote name.
FORK_REMOTE_NAME=fork

# Required: branch to push.
PUSH_BRANCH=feature/multi-auth-admin-review

# Required: base branch for PR.
PR_BASE_BRANCH=main

# Optional git identity (used if your global git identity is not set).
GIT_AUTHOR_NAME=Your Name
GIT_AUTHOR_EMAIL=you@example.com

# Optional PR metadata.
PR_TITLE=feat: improve rotation behavior, force mode, and dashboard strategy UX
PR_BODY_FILE=docs/ADMIN_MERGE_BRIEF.md

# SMSPool settings for auto-login phone verification.
# The real .env file is ignored by Git; never commit the API key.
SMSPOOL_API_KEY=replace_with_your_smspool_api_key
SMSPOOL_COUNTRY=1
SMSPOOL_SERVICE=671
# SMSPOOL_POOL=1
# SMSPOOL_MAX_PRICE=0.50
# SMSPOOL_PRICING_OPTION=0
# SMSPOOL_AREA_CODE=203
# SMSPOOL_EXCLUDE=0
# SMSPOOL_TIMEOUT=180
# SMSPOOL_MAX_ORDERS=3

# Optional browser proxy for standalone and dashboard-assisted auto-login.
# Keep credentials separate from the URL so they are never printed with it.
# OPENCODE_MULTI_AUTH_PROXY=http://proxy.example.com:3128
# OPENCODE_MULTI_AUTH_PROXY_USERNAME=replace_with_proxy_username
# OPENCODE_MULTI_AUTH_PROXY_PASSWORD=replace_with_proxy_password
# OPENCODE_MULTI_AUTH_PROXY_BYPASS=localhost,127.0.0.1,::1

# Optional quarantine path for deleted/deactivated OpenAI accounts.
# OPENCODE_MULTI_AUTH_DEACTIVATED_FILE=~/.config/opencode-multi-auth/deactivated-accounts.json
